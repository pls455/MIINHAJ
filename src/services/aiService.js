const AI_BASE=import.meta.env.VITE_AI_BASE_URL||'';
async function call(path,body,timeout=30000){const c=new AbortController();const t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(`${AI_BASE}${path}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),signal:c.signal});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`AI HTTP ${r.status}`);return data}finally{clearTimeout(t)}}
export const aiService={askStudyQuestion:(question,context=[])=>call('/ai',{question,context:context.slice(0,8)}),searchResourcesWithAI:(question,resources)=>call('/resource-search',{question,resources:resources.slice(0,10)}),classifyResource:(input)=>call('/classify',input),generateStudyPlan:(input)=>call('/study-plan',input),explainQuestion:(question,context=[])=>call('/ai',{question,context})};

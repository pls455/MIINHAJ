import { FormEvent, useState } from 'react';
import { Navigate, Link } from 'react-router-dom';
import { useAuth } from '../../app/AuthProvider';
export function AdminLogin(){const {user,signIn,loading}=useAuth();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');if(!loading&&user)return <Navigate to="/admin" replace/>;async function submit(e:FormEvent){e.preventDefault();setError('');try{await signIn(email,password)}catch{setError('بيانات الدخول غير صحيحة أو الحساب غير مخول.')}}return <section className="auth"><form className="card" onSubmit={submit}><h1>دخول الإدارة</h1><label>البريد<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>كلمة المرور<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button className="button">دخول</button><Link to="/">العودة</Link></form></section>}

import { mountShell } from '../components/layout.js';
import { auth } from '../services/firebase.js';
import { getStudentProfile, initStudentAuth, loginStudent, logoutStudent, registerStudent, saveStudentProfile, watchAuth, authErrorMessage } from '../services/studentAuth.js';
import { escapeHtml } from '../core/utils.js';

mountShell('حساب الطالب', `<style>
.account-wrap{max-width:760px;margin:0 auto;display:grid;gap:18px}.account-card{padding:26px;border:1px solid var(--color-border);border-radius:24px;background:var(--color-surface);box-shadow:var(--shadow-md)}.account-card h2{margin-top:0}.account-card p{color:var(--color-muted)}.account-form{display:grid;gap:14px}.account-form label{display:grid;gap:7px;font-weight:600}.account-form input{width:100%;box-sizing:border-box}.account-form button{width:max-content}.account-tabs{display:flex;gap:8px;margin-bottom:18px}.account-tabs button{border:1px solid var(--color-border);background:var(--color-surface-solid);border-radius:12px;padding:10px 16px;cursor:pointer}.account-tabs button.active{background:var(--color-primary);color:#fff;border-color:var(--color-primary)}.account-message{margin-top:12px}.account-badges{display:flex;flex-wrap:wrap;gap:8px}.account-badge{padding:7px 11px;border-radius:999px;background:var(--color-surface-solid);font-size:13px}.optional{font-weight:400;color:var(--color-muted);font-size:13px}.privacy-note{font-size:13px;padding:13px;border-radius:14px;background:var(--color-surface-solid)}
</style><section class="page-head"><span class="eyebrow">اختياري</span><h1>حساب الطالب</h1><p>استخدم منهاج كزائر بدون حساب، أو سجّل الدخول لفتح الميزات الشخصية وحفظ تجربتك.</p></section><div id="accountRoot" class="account-wrap"></div>`);

const root=document.getElementById('accountRoot');

function guestView(mode='login',message=''){
  const login=mode==='login';
  root.innerHTML=`<section class="account-card"><div class="account-tabs"><button type="button" data-mode="login" class="${login?'active':''}">تسجيل الدخول</button><button type="button" data-mode="register" class="${!login?'active':''}">إنشاء حساب</button></div>${login?`<form id="loginForm" class="account-form"><label>البريد الإلكتروني<input id="loginEmail" type="email" autocomplete="email" required placeholder="name@example.com"></label><label>كلمة المرور<input id="loginPassword" type="password" autocomplete="current-password" required minlength="6" placeholder="••••••••"></label><button class="button primary" type="submit">دخول</button></form>`:`<form id="registerForm" class="account-form"><label>الاسم<input id="registerName" maxlength="100" autocomplete="name" required placeholder="اسمك"></label><label>البريد الإلكتروني<input id="registerEmail" type="email" maxlength="200" autocomplete="email" required placeholder="name@example.com"></label><label>كلمة المرور<input id="registerPassword" type="password" minlength="6" autocomplete="new-password" required placeholder="6 أحرف على الأقل"></label><label>رقم الهوية <span class="optional">(اختياري)</span><input id="registerIdentity" inputmode="numeric" maxlength="32" autocomplete="off" placeholder="يمكنك تركه فارغًا"></label><div class="privacy-note">رقم الهوية اختياري، ويُحفظ داخل حسابك ولا يظهر للطلاب الآخرين. نستخدمه فقط عندما تحتاج ميزة تعليمية مخصصة إليه.</div><button class="button primary" type="submit">إنشاء الحساب</button></form>`}<p id="accountMsg" class="message account-message">${escapeHtml(message)}</p></section><section class="account-card"><h2>ماذا يفتح الحساب؟</h2><div class="account-badges"><span class="account-badge">حفظ التقدم</span><span class="account-badge">المفضلة</span><span class="account-badge">إحصائيات الدراسة</span><span class="account-badge">تخصيص الأدوات</span><span class="account-badge">مزامنة تجربتك</span></div><p>التصفح الأساسي والبطاقات والاقتراحات تبقى متاحة بدون تسجيل دخول.</p></section>`;
  root.querySelectorAll('[data-mode]').forEach(btn=>btn.addEventListener('click',()=>guestView(btn.dataset.mode)));
  root.querySelector('#loginForm')?.addEventListener('submit',handleLogin);
  root.querySelector('#registerForm')?.addEventListener('submit',handleRegister);
}

async function handleLogin(event){event.preventDefault();const button=event.submitter,msg=document.getElementById('accountMsg');button.disabled=true;try{await loginStudent(document.getElementById('loginEmail').value,document.getElementById('loginPassword').value);await renderUser()}catch(error){msg.textContent=authErrorMessage(error);msg.classList.add('error')}finally{button.disabled=false}}

async function handleRegister(event){event.preventDefault();const button=event.submitter,msg=document.getElementById('accountMsg');button.disabled=true;try{await registerStudent({name:document.getElementById('registerName').value,email:document.getElementById('registerEmail').value,password:document.getElementById('registerPassword').value,identityNumber:document.getElementById('registerIdentity').value});await renderUser()}catch(error){msg.textContent=authErrorMessage(error);msg.classList.add('error')}finally{button.disabled=false}}

async function renderUser(){const user=auth.currentUser;if(!user){guestView();return}const profile=await getStudentProfile(user.uid);root.innerHTML=`<section class="account-card"><span class="eyebrow">مسجل دخول</span><h2>أهلًا ${escapeHtml(profile?.name||user.displayName||'طالب')}</h2><p>${escapeHtml(user.email||'')}</p><div class="account-badges"><span class="account-badge">حساب طالب</span>${profile?.identityNumber?'<span class="account-badge">رقم الهوية محفوظ</span>':'<span class="account-badge">رقم الهوية غير مضاف</span>'}</div><form id="profileForm" class="account-form" style="margin-top:20px"><label>الاسم<input id="profileName" maxlength="100" required value="${escapeHtml(profile?.name||user.displayName||'')}"></label><label>رقم الهوية <span class="optional">(اختياري)</span><input id="profileIdentity" inputmode="numeric" maxlength="32" autocomplete="off" value="${escapeHtml(profile?.identityNumber||'')}"></label><div class="privacy-note">رقم الهوية يبقى خاصًا بحسابك ولا نعرضه في الصفحات العامة.</div><button class="button primary" type="submit">حفظ البيانات</button></form><button id="logout" class="button" type="button">تسجيل الخروج</button><p id="profileMsg" class="message" role="status"></p></section><section class="account-card"><h2>ميزات الحساب</h2><p>هذه البنية جاهزة لربط التقدم، المفضلة، الإحصائيات، والأدوات المخصصة بحساب الطالب بدون إجبار الزائر على التسجيل.</p></section>`;root.querySelector('#profileForm').addEventListener('submit',handleProfileSave);root.querySelector('#logout').addEventListener('click',async()=>{await logoutStudent();guestView()})}

async function handleProfileSave(event){event.preventDefault();const button=event.submitter,msg=document.getElementById('profileMsg');button.disabled=true;msg.classList.remove('error');try{await saveStudentProfile({name:document.getElementById('profileName').value,identityNumber:document.getElementById('profileIdentity').value});msg.textContent='تم حفظ بيانات الحساب.'}catch(error){msg.textContent=authErrorMessage(error);msg.classList.add('error')}finally{button.disabled=false}}

await initStudentAuth();watchAuth(user=>{if(user)renderUser();else if(!root.querySelector('#loginForm')&&!root.querySelector('#registerForm'))guestView()});if(auth.currentUser)await renderUser();else guestView();

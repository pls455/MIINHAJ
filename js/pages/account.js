import { mountShell } from '../components/layout.js';import { auth } from '../services/firebase.js';import { getStudentProfile,initStudentAuth,loginStudent,logoutStudent,registerStudent,saveStudentProfile,watchAuth,authErrorMessage } from '../services/studentAuth.js';import { escapeHtml } from '../core/utils.js';mountShell('حساب الطالب',`<section class="page-head"><span class="eyebrow">اختياري</span><h1>حساب الطالب</h1><p>استخدم منهاج كزائر أو سجّل الدخول لفتح الميزات الشخصية.</p></section><div id="accountRoot"></div>`);const root=document.getElementById('accountRoot');function guestView(mode='login'){const login=mode==='login';root.innerHTML=`<section class="card"><div class="account-tabs"><button type="button" data-mode="login" class="${login?'active':''}">تسجيل الدخول</button><button type="button" data-mode="register" class="${!login?'active':''}">إنشاء حساب</button></div>${login?`<form id="loginForm" class="form-card"><label>البريد الإلكتروني<input id="loginEmail" type="email" required></label><label>كلمة المرور<input id="loginPassword" type="password" minlength="6" required></label><button class="button primary">دخول</button></form>`:`<form id="registerForm" class="form-card"><label>الاسم<input id="registerName" maxlength="100" required></label><label>البريد الإلكتروني<input id="registerEmail" type="email" required></label><label>كلمة المرور<input id="registerPassword" type="password" minlength="6" required></label><label>رقم الهوية (اختياري)<input id="registerIdentity" inputmode="numeric" maxlength="32"></label><button class="button primary">إنشاء الحساب</button></form>`}<p id="accountMsg" class="message"></p></section>`;root.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>guestView(b.dataset.mode));root.querySelector('#loginForm')?.addEventListener('submit',handleLogin);root.querySelector('#registerForm')?.addEventListener('submit',handleRegister)}async function handleLogin(e){e.preventDefault();try{await loginStudent(root.querySelector('#loginEmail').value,root.querySelector('#loginPassword').value);await renderUser()}catch(err){root.querySelector('#accountMsg').textContent=authErrorMessage(err)}}async function handleRegister(e){e.preventDefault();try{await registerStudent({name:root.querySelector('#registerName').value,email:root.querySelector('#registerEmail').value,password:root.querySelector('#registerPassword').value,identityNumber:root.querySelector('#registerIdentity').value});await renderUser()}catch(err){root.querySelector('#accountMsg').textContent=authErrorMessage(err)}}async function renderUser(){const user=auth.currentUser;if(!user)return guestView();const profile=await getStudentProfile(user.uid);root.innerHTML=`<section class="card"><span class="eyebrow">مسجل دخول</span><h2>أهلًا ${escapeHtml(profile?.name||user.displayName||'طالب')}</h2><p>${escapeHtml(user.email||'')}</p><div class="actions"><a class="button primary" href="my-flashcards.html">🗂️ بطاقاتي</a><button id="logout" class="button">تسجيل الخروج</button></div></section>`;root.querySelector('#logout').onclick=async()=>{await logoutStudent();guestView()}}await initStudentAuth();watchAuth(user=>user?renderUser():guestView());if(auth.currentUser)await renderUser();else guestView();
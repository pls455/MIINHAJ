import { mountShell } from '../components/layout.js';import { db } from '../services/firebase.js';import { collection,addDoc,serverTimestamp } from 'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';import { setMessage,setBusy,qs } from '../core/utils.js';
mountShell('الإبلاغ عن مشكلة',`<form id="form" class="form-card"><label>المصدر<input id="title" readonly></label><label>نوع المشكلة<select id="kind"><option>الرابط لا يعمل</option><option>المصدر مصنف بشكل خاطئ</option><option>محتوى غير مناسب</option><option>أخرى</option></select></label><label>التفاصيل<textarea id="description" required maxlength="2000"></textarea></label><button class="button primary">إرسال البلاغ</button><p id="msg" class="message"></p></form>`);document.getElementById('title').value=qs.get('title')||qs.get('sourceId')||'';document.getElementById('form').addEventListener('submit',async e=>{e.preventDefault();const btn=e.submitter;setBusy(btn,true);try{await addDoc(collection(db,'problemReports'),{sourceId:qs.get('sourceId')||'',sourceTitle:document.getElementById('title').value,kind:document.getElementById('kind').value,description:document.getElementById('description').value.trim(),status:'open',createdAt:serverTimestamp()});setMessage(document.getElementById('msg'),'تم تسجيل البلاغ، شكرًا.')}catch(err){console.error(err);setMessage(document.getElementById('msg'),'تعذر تسجيل البلاغ.',true)}finally{setBusy(btn,false)}});
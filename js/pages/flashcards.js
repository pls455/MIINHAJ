import { mountShell } from '../components/layout.js';
import { escapeHtml } from '../core/utils.js';
import { createSuggestion } from '../repositories/suggestionRepository.js';
import { getPage } from '../repositories/resourceRepository.js';
mountShell('البطاقات', `<section class="page-head"><span class="eyebrow">مِنهَاج</span><h1>البطاقات التعليمية</h1><p>مراجعة البطاقات المنشورة في منهاج. ولإنشاء بطاقاتك الخاصة استخدم <a href="my-flashcards.html">بطاقاتي</a>.</p></section><section class="card"><div id="status" class="message" role="status"></div><div id="list" class="flashcard-wrap" aria-live="polite"></div><div class="actions"><button id="prev" class="button" type="button">السابق</button><button id="next" class="button primary" type="button">التالي</button></div><button id="more" class="button" type="button" hidden>تحميل بطاقات إضافية</button></section><section class="suggest-card"><h2>💡 اقترح بطاقة جديدة</h2><p>أرسل سؤالًا وإجابة للمراجعة قبل نشرها.</p><div id="suggestArea"></div></section>`);
const list=document.getElementById('list'),status=document.getElementById('status'),more=document.getElementById('more'),prev=document.getElementById('prev'),next=document.getElementById('next'),suggestArea=document.getElementById('suggestArea');let rows=[],index=0,cursor=null,loading=false,repositoriesPromise;const seenIds=new Set();const palettes=[['linear-gradient(135deg,#4f46e5,#7c3aed)','linear-gradient(135deg,#4338ca,#6d28d9)'],['linear-gradient(135deg,#0284c7,#06b6d4)','linear-gradient(135deg,#0369a1,#0e7490)'],['linear-gradient(135deg,#059669,#14b8a6)','linear-gradient(135deg,#047857,#0f766e)']];function getRepositories(){return repositoriesPromise??=import('../repositories/resourceRepository.js').then(m=>m.repositories)}function render(){const item=rows[index];if(!item){list.innerHTML='<div class="empty">لا توجد بطاقات متاحة حاليًا.</div>';return}const [bg,answerBg]=palettes[index%palettes.length];list.innerHTML=`<article class="flashcard" tabindex="0" role="button" aria-label="اضغط لقلب البطاقة" style="--card-bg:${bg};--card-answer-bg:${answerBg}"><div class="flashcard-inner"><div class="flashcard-face question"><span class="eyebrow">السؤال</span><span class="flashcard-count">${index+1} / ${rows.length}</span><h2>${escapeHtml(item.question||'')}</h2><span class="flashcard-hint">اضغط لإظهار الإجابة</span></div><div class="flashcard-face answer"><span class="eyebrow">الإجابة</span><span class="flashcard-count">${index+1} / ${rows.length}</span><p>${escapeHtml(item.answer||'')}</p>${item.explanation?`<small class="flashcard-hint">${escapeHtml(item.explanation)}</small>`:''}<span class="flashcard-hint">اضغط للعودة للسؤال</span></div></div></article>`;const card=list.querySelector('.flashcard');const flip=()=>card.classList.toggle('is-flipped');card.onclick=flip;card.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();flip()}}}async function loadMore(){if(loading)return;if(rows.length&&!cursor)return;loading=true;more.disabled=true;try{const repositories=await getRepositories();const result=await repositories.flashcards.page({},30,cursor);const fresh=result.rows.filter(x=>!seenIds.has(x.id));fresh.forEach(x=>seenIds.add(x.id));rows.push(...fresh);cursor=result.nextCursor;more.hidden=!result.hasMore;status.textContent='';render()}catch(error){console.error(error);status.textContent='تعذر تحميل البطاقات.'}finally{loading=false;more.disabled=false}}async function renderSuggestionForm(){try{const {rows:branches=[]}=await getPage('branches',{active:true},100);const {rows:subjects=[]}=await getPage('subjects',{active:true},100);suggestArea.innerHTML=`<form id="suggestForm" class="form-grid"><label>الفرع<select id="suggestBranch" required><option value="">اختر الفرع</option>${branches.map(x=>`<option value="${escapeHtml(x.id)}">${escapeHtml(x.name||x.title||x.id)}</option>`).join('')}</select></label><label>المادة<select id="suggestSubject" required><option value="">اختر المادة</option>${subjects.map(x=>`<option value="${escapeHtml(x.id)}">${escapeHtml(x.name||x.title||x.id)}</option>`).join('')}</select></label><label>السؤال<textarea id="suggestQuestion" rows="3" maxlength="1000" required></textarea></label><label>الإجابة<textarea id="suggestAnswer" rows="4" maxlength="3000" required></textarea></label><button class="button primary" type="submit">إرسال للمراجعة</button><p id="suggestMsg" class="message"></p></form>`;document.getElementById('suggestForm').onsubmit=async e=>{e.preventDefault();const b=e.submitter,msg=document.getElementById('suggestMsg');b.disabled=true;try{await createSuggestion({title:document.getElementById('suggestQuestion').value,contentType:'flashcard',studentName:'',branchId:document.getElementById('suggestBranch').value,subjectId:document.getElementById('suggestSubject').value,question:document.getElementById('suggestQuestion').value,answer:document.getElementById('suggestAnswer').value,url:'',type:'flashcard',description:document.getElementById('suggestAnswer').value,keywords:[]});msg.textContent='تم إرسال البطاقة للمراجعة.';e.target.reset()}catch(err){msg.textContent='تعذر إرسال البطاقة.';msg.classList.add('error')}finally{b.disabled=false}}}catch(error){suggestArea.innerHTML='<div class="message error">تعذر تحميل نموذج الاقتراح.</div>'}}prev.onclick=()=>{if(rows.length){index=index?index-1:rows.length-1;render()}};next.onclick=()=>{if(rows.length){index=index+1<rows.length?index+1:0;render()}};more.onclick=loadMore;loadMore();renderSuggestionForm();

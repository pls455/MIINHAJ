import { mountShell } from '../components/layout.js';import { getAllSmall } from '../repositories/resourceRepository.js';import { escapeHtml } from '../core/utils.js';mountShell('الفروع',`<div id="list" class="grid"></div>`);try{const rows=await getAllSmall('branches');document.getElementById('list').innerHTML=rows.map(x=>`<a class="card branch-card" href="subjects.html?branch=${encodeURIComponent(x.id)}"><span>${escapeHtml(x.icon||'🌿')}</span><h2>${escapeHtml(x.name)}</h2><p>${escapeHtml(x.description||'')}</p></a>`).join('')||'<div class="empty">لا توجد فروع متاحة.</div>'}catch(e){console.error(e);document.getElementById('list').innerHTML='<div class="error-box">تعذر تحميل الفروع.</div>'}
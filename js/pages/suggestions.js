import { mountShell } from '../components/layout.js';import { db } from '../services/firebase.js';import { collection,addDoc,serverTimestamp } from 'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';import { setMessage,setBusy } from '../core/utils.js';
mountShell('إرسال اقتراح',`<form id="form" class="form-card"><label>العنوان<input id="title" required maxlength="160"></label><label>النوع<select id="contentType"><option value="resource">مصدر</option><option value="foundation">تأسيس</option></select></label><label>الرابط<input id="url" type="url" placeholder="https://..."></label><label>التفاصيل<textarea id="description" maxlength="3000"></textarea></label><button class="button primary">إرسال الاقتراح</button><p id="msg" class="message"></p></form>`);document.getElementById('form').addEventListener('submit',async e=>{e.preventDefault();const btn=e.submitter;setBusy(btn,true);try{await addDoc(collection(db,'suggestions'),{title:document.getElementById('title').value.trim(),contentType:document.getElementById('contentType').value,url:document.getElementById('url').value.trim(),description:document.getElementById('description').value.trim(),status:'pending',createdAt:serverTimestamp()});setMessage(document.getElementById('msg'),'تم إرسال الاقتراح للمراجعة.')}catch(err){console.error(err);setMessage(document.getElementById('msg'),'تعذر إرسال الاقتراح.',true)}finally{setBusy(btn,false)}});
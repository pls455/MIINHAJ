const AI_URL=window.MINHAJ_AI_URL||'https://json2.karamhahhss123.workers.dev';
async function request(path,payload){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),28000);try{const r=await fetch(`${AI_URL}${path}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload),signal:controller.signal});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`AI HTTP ${r.status}`);return data}finally{clearTimeout(timer)}}
export const askStudyQuestion=(question,context=[])=>request('/ai',{question,context});export const searchResourcesWithAI=(question,resources=[])=>request('/resource-search',{question,resources});export const classifyResource=input=>request('/classify',input);export const generateStudyPlan=input=>request('/study-plan',input);
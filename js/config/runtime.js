// Runtime configuration for deployed services.
// Keep service URLs separate from application logic so deployments can update them
// without coupling the AI client to a hard-coded endpoint.
window.MINHAJ_AI_URL = 'https://minhaj1.karamhahhss123.workers.dev';
